// wow 

fn add(x:i32, y:i32) -> i32 {
    return x + y;
}
fn fun(x:i32, y:i32) -> i32 {
    let a: i32 = 69; 
    let b: i32 = 420;
    return (x-a) * (y+b);
}
fn main() -> i32 {
    // i just keep thinking about how much we are leaning on C's runtime here.
    let a: i32 = add(2,5);
    let b: i32 = fun(2,5);
    say a; say "\n";
    say b; say "\n";
    //let b: i32 = 0;
    //b = add(2,5);
    return 0;
}
